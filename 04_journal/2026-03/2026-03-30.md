# 2026-03-30（月）

## 実施内容

### SIFTAI 週次HTMLレポート Mac側実行対応
- Windows側タスクスケジューラ（9:32予定）が実行されず、HTMLレポートがSlack未投稿だった
- `weekly_member_analysis.py` と `channel_log_analysis.py` の `get_access_token()` をOAuth認証対応に書き換え
  - SA（サービスアカウント）優先 → なければOAuth pickle → なければ新規認証フロー
  - Windows/Mac両対応になった
- レポート保存ディレクトリ（週次レポート/）をMac側に作成
- Slackユーザートークンに `files:write` スコープ追加 → 再インストール → トークン更新
- `weekly_member_analysis.py --combined` を手動実行 → Slack投稿完了（PRO/PREMIUM/合算/チャンネルログ分析の4ファイル）

## 決定事項
- Google Sheets認証はSA優先・OAuth fallbackの2段構え方式を採用
- Mac側からも週次レポート生成・Slack投稿が可能になった

### Slack投稿方式をGitHub Pages URL方式に変更
- ファイルアップロード方式 → GitHub PagesのURLリンク投稿方式に変更
- HTMLをgit push → GitHub Pagesで自動公開 → URLをSlackに投稿
- ベースURL: `https://saepin0519.github.io/ai-agent/`
- URL方式での初回Slack投稿完了（3/23〜3/29分）

## 次回への申し送り
- Windows側タスクスケジューラが動かなかった原因を確認（PC未起動？）
- Mac側でもcron等で自動実行するか検討
- 週次レポート自動実行時はgit push（GitHub Pages反映）もフローに含める必要あり
- Slackトークン（`slack_user_token.txt`）は `files:write` スコープ追加済みの新トークンに更新済み
